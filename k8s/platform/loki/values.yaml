---
loki:
  loki:
    auth_enabled: false
    commonConfig:
      replication_factor: 1
    storage:
      type: "filesystem"
    analytics:
      reporting_enabled: false
  singleBinary:
    replicas: 1
